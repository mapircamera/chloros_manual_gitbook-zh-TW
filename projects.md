# 图形用户界面：项目

Chloros 允许您创建项目，以便日后重新打开。项目是一个普通文件夹（位于您的“项目文件夹”内），其中包含：

* `project.json` — 项目设置、文件列表和显示偏好设置
* `cameras.json` — 项目打开期间连接的摄像头和阵列，以及其设置
* `sensors.json` — 项目打开期间连接的 DAQ 光传感器，以及相机与传感器之间的绑定
* 您的捕获数据、`.daq` 录制文件以及处理后的输出文件夹

本软件不采用专有项目文件格式——整个文件夹及其中的 JSON 文件即构成项目，这也使得项目易于复制、归档，并可通过 [CLI](CLI.md) 或 [Python SDK](api-python-sdk.md) 运行。

## 新建项目

<figure><img src=".gitbook/assets/image (26).png" alt=""><figcaption></figcaption></figure>从主菜单中选择“新建项目”，并为项目输入一个唯一的名称。

如果您已保存任何项目模板，名称字段下方会显示一个**“选择模板”**下拉菜单——选择其中一个即可根据该模板的设置开始新建项目。 模板可通过 [项目设置](project-settings/project-settings.md) 保存：在“保存项目模板名称”字段中输入名称，然后点击保存图标。

## 打开项目

<figure><img src=".gitbook/assets/v120-open-project.jpg" alt=""><figcaption><p>“打开项目”列出了项目文件夹中的所有项目，底部设有<strong>“打开项目文件夹”</strong>选项</p></figcaption></figure>选择“打开项目”可查看项目文件夹中现有的项目列表。如果不存在任何项目，则次级侧边菜单不会打开。 如上图所示，您可以看到一些由图形界面创建的项目（t1、t2、t3）。以“DATE\_TIME”命名的项目是由 CLI 根据默认项目命名规则生成的。点击任意项目名称即可打开该项目。

点击“打开项目文件夹”按钮，将打开计算机文件资源管理器并跳转至项目路径。您可以在 [项目设置](project-settings/project-settings.md) 中调整项目路径。

如果自上次打开项目以来，项目中的任何源图像文件已被移动或删除，Chloros 会显示一个对话框，列出确切缺失的文件，而不是打开一个空白网格。

## 复制项目

此功能在项目打开后可用。选择“复制项目”可将当前项目以新名称复制——Chloros会建议下一个可用名称（例如“MyProject (2)”）——复制后的项目将立即打开。

## 添加文件

打开项目后，从主菜单中选择“添加文件”可将单个图像文件添加到当前项目中。此功能与文件浏览器的添加功能相同，但为方便起见，可直接从主菜单访问。

## 添加文件夹

打开项目后，从主菜单中选择“添加文件夹”，即可将包含图像的文件夹添加到当前项目中。您可以一次性选择多个文件夹。重复文件将被忽略。

## 开始/停止处理

文件添加到项目后，主菜单中将出现“开始处理”选项。此操作与点击顶部标题栏中的“播放/开始”按钮效果相同。处理过程中，该菜单项将变为“停止处理”，以便您中止处理流程。

## 连接相机 / 连接光传感器

主菜单底部有两个硬件快捷方式，无论项目是否打开均可使用：

* **连接相机** — 打开 [相机选项卡](lattice/) 以连接 LATTICE 相机或阵列。
* **连接摄像头** — 打开 [摄像头选项卡](lattice/) 以连接 LATTICE 摄像头或摄像头阵列。

在项目打开时连接硬件会将设置保存到该项目中（见下文）。若未打开项目，连接仅在当前会话中有效。

{% hint style="info" %}
“添加文件”、“添加文件夹”以及“开始/停止处理”菜单项仅在打开项目且已添加文件时才会显示或启用。它们可快速访问“文件浏览器”侧边栏和标题栏按钮中提供的操作。
{% endhint %}

## 项目会记住您的硬件

1.2.0 版新增功能：项目在打开期间会保留您连接的硬件。 摄像头和阵列（包括其每台摄像头的设置、名称、颜色和网格布局）会被快照保存到 `cameras.json` 中，而光传感器（包括名称、颜色和摄像头绑定）则会被保存到 `sensors.json` 中 ——这些操作会在您工作时自动完成。

当您**重新打开**项目时，Chloros 不会立即连接任何硬件。每个部分会在您首次访问其所属标签页时重新连接：

* 打开 **相机** 标签页时，将重新连接已保存的相机和阵列，并重新应用其已保存的设置。
* 打开 **光传感器** 选项卡时，将重新连接已保存的 DAQ 传感器。

这样一来，仅为浏览或导出图像而打开项目时，绝不会触发摄像头的流式传输。如果在打开选项卡时无法找到已保存的设备，系统会弹出对话框告知您哪些设备不可用，以便您重新连接或将其移除。

## 项目中的 DAQ 录制数据和 .daq 文件

* 在项目打开期间（通过“光传感器”选项卡或捕获过程中）生成的 `.daq` 录制数据将 **自动添加到项目中**。
* 导入的 `.daq` 文件以及所有项目记录，都会列在 [项目设置](project-settings/project-settings.md) 的 **DAQ 光传感器** 部分中，每项都附有相应的顶部校正配置文件。
* 在处理过程中，项目中的 `.daq` 文件为反射率产品提供下射照度——请参阅 [输出图像格式](output-image-formats.md)。

## 无界面运行已保存的项目

保存的项目可在不使用图形用户界面（GUI）的情况下运行：

* **CLI**：`chloros-cli project open / connect / capture / sensor / align / run` 基于项目文件夹路径运行——请参阅 [CLI 参考](reference/cli-reference.md)。
* **SDK**：`chloros_sdk.open_project(path)` 返回一个项目句柄； `connect_all()` 将所有已保存的摄像头和传感器及其保存的设置恢复为在线状态 — 请参阅 [SDK 参考](reference/sdk-reference.md)。
